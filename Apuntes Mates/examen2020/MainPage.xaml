<Page
    x:Class="Examen_4.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Examen_4"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style x:Key="Botones" TargetType="Button">
            <Setter  Property="BorderThickness" Value="5"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="Width" Value="375"/>
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="Height" Value="100"/>
        </Style>
    </Page.Resources>      
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="75">
            <Button Content="Jugar" Style="{StaticResource Botones}" Click="JugarButton_Click"/>
            <Button Content="Configuracion" Style="{StaticResource Botones}" Click="ConfiguracionButton_Click"/>
            <Button Content="Salir" FontSize="48" Style="{StaticResource Botones}" Click="SalirButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
